<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <global-method-security pre-post-annotations="enabled"/>

    <http pattern="/static/**" security="none"/>

    <http auto-config="true" use-expressions="true">
        <form-login authentication-failure-url="/login?login_error=t"
                    login-page="/login" login-processing-url="/static/j_spring_security_check"/>

        <logout logout-url="/static/j_spring_security_logout"/>
        <logout logout-success-url="/logout" delete-cookies="JSESSIONID"/>
        <intercept-url pattern="/login/**" access="permitAll"/>
        <intercept-url pattern="/admin/*" access="hasAuthority(admin)"/>
        <intercept-url pattern="/user/*" access="hasAuthority(user)"/>

        <session-management invalid-session-url="/timeout">
            <concurrency-control max-sessions="1" error-if-maximum-exceeded="true"/>
        </session-management>

    </http>

    <!-- Configure Authentication mechanism -->
    <beans:bean class="org.jugbd.mnet.security.DatabaseAuthenticationProvider"
                id="databaseAuthenticationProvider">
        <beans:property name="adminUser" value="admin"/>
        <beans:property name="adminPassword" value="8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918"/>
    </beans:bean>

    <authentication-manager alias="authenticationManager">
        <!-- SHA-256 values can be produced using 'echo -n your_desired_password
            | sha256sum' (using normal *nix environments) -->
        <authentication-provider ref="databaseAuthenticationProvider">
            <password-encoder hash="sha-256"/>
        </authentication-provider>
    </authentication-manager>

    <beans:bean class="org.springframework.security.authentication.encoding.MessageDigestPasswordEncoder"
                id="messageDigestPasswordEncoder">
        <beans:constructor-arg value="sha-256"/>
    </beans:bean>

</beans:beans>
